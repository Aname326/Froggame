$('gameplay.html').ready(function() {
    $('body').on(keydown, function() {
        var j = e.keyCode || e.which;

        if (j == "A") {
            ('meanupage').click()
        }
            
    }
});

